/**
 * Create a context provider file.
 * Create a context and set Provider.
 * Set the children props.
 * Set the value.
 * Set Provider.
 * set register.
 * set sign in.
 * set sign out.
 * set onAuthStateChange(auth, user =>) for keep user in site.
 * set private route.
 */